// Quzz :
// 1) declare a variable with the type string.
let test_str = ""; // Empty but string

// 2) declare a variable that can be either a string or a number.
let age: string | number = "";

// 3) declare a variable that can be either a string or a null.
let mob_number: string | null = null;

// 4) declare an uninitialized varible with the type of string.
let set_str: string | undefined;
